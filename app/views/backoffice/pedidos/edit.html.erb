<div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">                  
                  
                    <%= form_for([:backoffice, @pedido]) do |f| %>

                      <div class="form-group">
                         <label>Status</label>
                         <%= f.number_field :status, class: "form-control", placeholder: "Ex.: 1: aguardando - 2: Finalizado" %>
                      </div>

                      <button type="submit" class="btn btn-default">Salvar</button>
                    <% end %>

                </div>
         </div>
  </div>
